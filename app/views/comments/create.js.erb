$(".<%= @comment.commentable.class.name.downcase %>-<%= @commentable.id %>-comment-errors").html('<%= render 'shared/errors', resource: @comment %>');

<% if !@comment.errors.present? %>
  $('#comments_<%= "#{@commentable.class.name.downcase}"%>_<%= @commentable.id %>').html('<%= j render @commentable.comments %>');
<% end %>