$('#question-errors_<%= @question.id %>').html('<%= render 'shared/errors', resource: @question %>')

<% if !@question.errors.present? %>
  $('#question_<%= @question.id %>').html('<%= j render @question %>');
<% end %>